import React from 'react'\nimport Head from 'next/head'\nimport Link from 'next/link'\nimport { useRouter } from 'next/router'\nimport { useEditorStore } from '@/store/editorStore'\n\nexport default function Preview() {\n  const router = useRouter()\n  const elements = useEditorStore((state) => state.elements)\n\n  const renderElement = (element: any) => {\n    switch (element.type) {\n      case 'heading':\n        return (\n          <h1 key={element.id} className=\"text-4xl font-bold mb-4 text-gray-800\">\n            {element.content}\n          </h1>\n        )\n      case 'paragraph':\n        return (\n          <p key={element.id} className=\"text-lg text-gray-700 mb-4\">\n            {element.content}\n          </p>\n        )\n      case 'button':\n        return (\n          <button\n            key={element.id}\n            className=\"px-6 py-3 bg-blue-500 text-white rounded-lg font-bold hover:bg-blue-600 mb-4\"\n          >\n            {element.content}\n          </button>\n        )\n      default:\n        return null\n    }\n  }\n\n  return (\n    <>\n      <Head>\n        <title>معاينة الموقع - Bankikuw</title>\n      </Head>\n\n      <div className=\"min-h-screen bg-gray-50\">\n        {/* Header */}\n        <header className=\"bg-white shadow sticky top-0 z-10\">\n          <div className=\"max-w-5xl mx-auto px-6 py-4 flex justify-between items-center\">\n            <h1 className=\"text-2xl font-bold text-blue-600\">👁️ معاينة الموقع</h1>\n            <div className=\"flex gap-3\">\n              <button\n                onClick={() => router.back()}\n                className=\"px-4 py-2 bg-gray-300 text-gray-800 rounded hover:bg-gray-400 transition-colors\"\n              >\n                ← العودة\n              </button>\n              <button\n                onClick={() => alert('✅ تم نشر الموقع بنجاح!')}\n                className=\"px-4 py-2 bg-green-500 text-white rounded hover:bg-green-600 transition-colors\"\n              >\n                🚀 نشر الآن\n              </button>\n            </div>\n          </div>\n        </header>\n\n        {/* Content */}\n        <main className=\"max-w-5xl mx-auto px-6 py-12\">\n          {elements.length === 0 ? (\n            <div className=\"text-center py-20\">\n              <p className=\"text-gray-500 text-lg mb-4\">لا توجد عناصر لعرضها</p>\n              <Link\n                href=\"/editor\"\n                className=\"inline-block px-6 py-3 bg-blue-500 text-white rounded-lg hover:bg-blue-600\"\n              >\n                ← العودة للمحرر\n              </Link>\n            </div>\n          ) : (\n            <div className=\"bg-white rounded-lg shadow p-8\">\n              {elements.map((element) => renderElement(element))}\n            </div>\n          )}\n        </main>\n      </div>\n    </>\n  )\n}\n